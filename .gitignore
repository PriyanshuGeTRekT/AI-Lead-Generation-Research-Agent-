# Environment (never commit secrets)
.env
*.env

# Data (generated at runtime, not source code)
data/chroma_db/
data/leads.json
data/metrics.jsonl
data/logs/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Node (pptxgenjs was used for architecture slides, no longer needed)
node_modules/
npm-debug.log*
package.json
package-lock.json

# Docker
.docker/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets / keys
*.pem
*.key
credentials.json

# Generated presentations (binary, regenerated from scripts/)
*.pptx
*.pdf

# runtime secrets / caches (never commit)
data/runtime_config.json
data/lead_cache.json
data/flywheel.json
data/tracking.json

# Large local data / DB (do not push — regenerate via harvest; see STARTUP_INDIA_PLAYBOOK.md)
data/warehouse.db
data/warehouse.db-wal
data/warehouse.db-shm
data/*.db
data/_si862.json
data/_si50_agents.json
